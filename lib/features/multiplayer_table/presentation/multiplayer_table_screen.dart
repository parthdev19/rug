/// Screen for the multiplayer_table feature.
library;

import 'package:flutter/material.dart';

/// MultiplayerTable screen placeholder.
/// TODO: Implement the multiplayer_table UI.
class MultiplayerTableScreen extends StatelessWidget {
  const MultiplayerTableScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'MultiplayerTable',
          style: Theme.of(context).textTheme.headlineMedium,
        ),
      ),
    );
  }
}
